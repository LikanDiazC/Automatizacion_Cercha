# Dependencias MÍNIMAS para arrancar el backend cuando hay poco espacio en disco.
# NO incluye: spacy, playwright, camoufox (scraping), cryptography pesado.
#
# Instalar con:
#   python -m pip install --no-cache-dir -r requirements-minimal.txt
#
# Una vez liberado espacio, pasar a requirements.txt completo.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
httpx
slowapi>=0.1.9
apscheduler>=3.10.0
# IA / unidades (livianos — necesarios para Capa 1 y 1.25)
quantulum3>=0.9.0
pint>=0.24
# Auth básico (si el router lo importa)
authlib>=1.3.0
itsdangerous>=2.2.0
bleach>=6.1.0
# Testing
pytest>=8.0.0
